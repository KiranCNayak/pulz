# Decisions Log — Pulz

Quick-reference list of decisions already made and closed during design.
If you're an agent or contributor picking this up: **treat these as
settled** — don't relitigate them without a new, explicit instruction from
the project owner. Each entry says what was decided and why, so you can
tell a "settled trade-off" apart from an "open question" (see
`ARCHITECTURE.md` §8 and `DESIGN.md` §8 for what's still genuinely open).

| # | Decision | Why | Where detailed |
|---|---|---|---|
| 1 | Scoring is **time-bracket based**, not rank-based | Rank-based scoring needs a strict global ordering of near-simultaneous answers across variable network latency — race-condition-prone. Time-bracket scoring only needs each player's own elapsed time. | PRD §5 |
| 2 | Scoring buckets are 5 tiers at 20%-of-time-limit steps, multiples of a base-10 point value, always non-negative integers | Explicit requirement: no fractional/negative scores, no "ugly" point values like 10 vs 25. | PRD §5, DESIGN.md §5 |
| 3 | Default question time limit: 20 seconds, configurable 5–120s per question | Matches an industry-validated default (Kahoot! itself defaults to 20s); configurable so text-heavy questions aren't shortchanged. | PRD §4.1 |
| 4 | No native mobile app for MVP | Responsive web covers "play from any device" without app-store overhead. | PRD §2, §7 |
| 5 | Host is split into two views: **Controller** (interactive) and **Display** (cast-friendly, read-only) | Matches real usage (host laptop + projected shared screen); both are just different subscriptions to the same event stream, not separate backend concepts. | PRD §4.2, DESIGN.md §6 |
| 6 | Question and answer-option order are randomized **per session**, not per player | Shared-screen "look at the shape" gameplay requires everyone in a session to see the same order. | DESIGN.md §4 |
| 7 | Podium (top 3, gold/silver/bronze, confetti/animation) is in MVP, not deferred | Low implementation cost (presentation-layer only), high payoff for the "aha" moment. | PRD §4.4 |
| 8 | Players see only their **own rank** (`"X of Y"`), never the full leaderboard | Explicit scope reduction: smaller per-player payload, avoids exposing other players' standings. | PRD §6, DESIGN.md §3 |
| 9 | No login for Host or Player/Spectator — only **Creators** have accounts | Keeps the common-case flow (join and play) frictionless. | PRD §3 |
| 10 | Join model: internal session UUID + short **6-character alphanumeric** join code | UUID is the unguessable "real" identifier (used for the results link); the short code is what a human actually types. Raised from an initial 4-character design (~1.68M combinations, brute-forceable in seconds) to 6 characters (~2.18 billion combinations) once the always-on/no-auth abuse-resilience requirement surfaced. | PRD §4.2, §8 |
| 11 | Pre-start joiners become **Players**; post-start joiners default to **Spectators** | Prevents a late joiner from disrupting an in-progress question, while still letting them watch. | PRD §4.3 |
| 12 | Spectator → Player promotion requires **explicit per-request Host approval**; no bulk-approve in MVP | Simpler to build and reason about; bulk approve is a listed post-MVP nice-to-have. | PRD §4.3, §9 |
| 13 | Promoted spectators do **not** get retroactive points for questions missed while spectating | Avoids retroactive score recomputation complexity. | PRD §4.3 |
| 14 | Results snapshot is ephemeral with a **configurable TTL, default 24h** | Explicit requirement to minimize storage cost/duration; default chosen as a reasonable "long enough to share, short enough to not accumulate." | PRD §4.4, §9 |
| 15 | TTL cleanup implemented as an **in-process interval sweep**, not a separate cron/scheduled service | Zero additional infrastructure; read path also defensively filters on `expires_at` so correctness never depends on the sweep's timing. | ARCHITECTURE.md §3 |
| 16 | Live `GameSession`/`Participant`/`Answer` state lives **in server process memory**, not the DB, while a game is in progress | Cheapest and fastest option at MVP scale; accepted trade-off is state loss on server restart mid-game. | ARCHITECTURE.md §2 |
| 17 | Backend: Node.js + TypeScript, Fastify (REST) + Socket.IO (realtime, rooms = sessions) | Rooms map directly onto sessions; Socket.IO's built-in reconnect/backoff covers the "phone drops mid-question" requirement without hand-rolling it. | ARCHITECTURE.md §1 |
| 18 | No horizontal scaling / multi-instance session routing in MVP | Would require moving session state out of process memory (e.g. to Redis) and adding sticky routing — real infra cost the PRD asked to avoid; revisit only if actually needed. | ARCHITECTURE.md §8 |
| 19 | Direct pushes to `main`, no PR-gated workflow, for this phase | Explicit project-owner instruction; revisit if the contributor pool grows beyond one. | This doc |
| 20 | Project name: **Pulz** | Chosen by project owner; merges "pulse" (real-time/speed scoring) and "quiz." | — |
| 21 | No blanket per-IP bans for abuse response; abuse handling is **temporary and composite-key-scoped** (IP + lightweight client id), not a persistent IP blacklist | IPs are shared (NAT/school Wi-Fi/CGNAT) and trivially rotated by an actual attacker — a blanket ban both over-punishes innocent shared-IP users and under-deters a real attacker. | PRD §8, ARCHITECTURE.md §11 |
| 22 | Anonymous-abuse hardening is scoped to **join / WebSocket connect / in-game messages only**, not session creation | Session creation already requires an authenticated Creator (Decision #9), so it's not part of the anonymous attack surface. | PRD §8, DESIGN.md §8 |
| 23 | Join-code brute-forcing is defeated by a **per-code failed-attempt lockout** (temporary), independent of and in addition to the 6-character length increase (#10) | Length alone only raises the cost of a blind guess; a targeted attacker hammering one known-active code still needs to be stopped directly. | DESIGN.md §8 |
| 24 | Edge/DDoS protection via **Cloudflare free tier** in front of both frontend and backend, with conditional Turnstile challenges (not shown to every user) | Outsources L3/L4 DDoS absorption to infrastructure built for it, at zero cost, without adding friction to the common case of a legitimate player joining. | ARCHITECTURE.md §11 |
| 25 | ORM/query layer: **Prisma** | Lightweight, TypeScript-first, generates types from schema (matches "one language, minimal moving parts" theme in ARCHITECTURE.md §1), and its migration model gives a durable, reviewable history for the durable entities (Creator/Quiz/Question/Option/ResultsSnapshot) without hand-rolling SQL migrations end to end. Trade-off accepted: Prisma's schema language has no native `@@check`/numeric-CHECK syntax, so the `time_limit_seconds` range constraint and the "at most one correct option" partial unique index are hand-added in the migration SQL rather than expressed in `schema.prisma` itself — documented inline in both files so it isn't lost on the next `prisma migrate dev`. | backend/prisma/schema.prisma, backend/prisma/migrations/0001_init/migration.sql |
| 26 | Backend lives in a top-level **`backend/`** directory (monorepo-by-convention, not a workspace tool) | Mirrors ARCHITECTURE.md §4's split frontend/backend deploys — a sibling `frontend/` will hold the React+Vite SPA (step 2) with its own independent deploy, without needing npm/pnpm workspaces machinery for what's currently a two-package repo. | This doc, ARCHITECTURE.md §4-§5 |
| 27 | `src/` split into `routes/` (Fastify HTTP handlers + JSON schemas), `sockets/` (Socket.IO handlers), `services/` (business logic incl. validation), `db/` (Prisma client + TTL sweep), `domain/` (shared constants), `types/` (request/response DTOs) | Keeps the "validation is server-authoritative" and "exactly-one-correct-option" invariants (CLAUDE.md, DESIGN.md §1) in one reusable service layer rather than duplicated across REST handlers and (later) socket handlers. | backend/src/ |
| 28 | Creator identity for the quiz-CRUD REST endpoints is currently a placeholder **`x-creator-id` request header**, not real auth | ARCHITECTURE.md §7's email/password (or JWT) Creator auth hasn't been built yet; rather than leave ownership scoping unimplemented (and every quiz effectively public/unscoped) until auth lands, the CRUD layer already takes and enforces a creator id, just from a stand-in header. **Must be replaced** with session/JWT-derived identity before this leaves scaffolding — tracked as follow-up work, not a scope decision to keep. | backend/src/routes/quiz.route.ts |
| 29 | "Exactly one correct option per question" enforced at the **application layer** (service-layer check on every create/update), with a **partial unique index** (`WHERE is_correct = true`) as a DB-level backstop against "more than one" | Matches DESIGN.md §1's explicit guidance ("app-level check on write; consider a partial unique index"). The partial unique index alone can't guarantee "at least one," so the app-layer check remains authoritative for the full invariant. | DESIGN.md §1, backend/src/services/quiz.service.ts |
| 30 | Question `time_limit_seconds` clamped to `[5, 120]` both in the application layer (on every write) and via a DB `CHECK` constraint | Belt-and-suspenders per the task's validation requirements: the app clamps rather than rejects (so a slightly-out-of-range value from a client just gets pulled into range instead of failing the whole request), while the DB CHECK guarantees the invariant holds even for a write that bypasses the app layer. | PRD §4.1, backend/prisma/migrations/0001_init/migration.sql || 31 | Start a **parallel, non-shipping `backend-go/` module** to measure Go vs. Node performance for this workload | Owner-driven curiosity about real performance gains; explicitly not a migration plan, not scheduled against MVP work, doesn't block frontend/other MVP steps. Stack: `chi` router, `pgx`+`sqlc` for DB (no ORM, to avoid confounding the perf measurement), `hey` for load testing — all installed as user-scoped `go install` tools, nothing system-wide. | `docs/GO_V2_EXPLORATION.md`, PRD §10 |
| 32 | Commit signing (`commit.gpgsign`) is set to **`false`, locally for this laptop's clone only** | This laptop's global git config signs commits by default, and the GPG agent's cached passphrase isn't always available (e.g. mid-drive, unattended). The project owner explicitly approved skipping signature verification here. Global git config (other repos) is untouched. If a *different* clone/environment (e.g. a fresh cloud-agent sandbox) hits the same GPG-signing block, it likely means that environment's own global config also defaults to signing — apply the same local override there, or ask the project owner. | `.git/config` (local, not repo-tracked — a fresh clone won't inherit this by default) |
| 33 | DESIGN.md's per-question state machine (`QUESTION_LOCKED` → host "next" → `LEADERBOARD_SHOWN` → host "next" → next question, three host actions) is implemented as **two** host-triggered socket events instead of three | `host:lock_question` now locks the question, computes scores, reveals the correct answer, *and* shows the leaderboard as one atomic step; `host:next_question` then advances to the next question or ends the game. Confirmed with the project owner: same information reaches every client, fewer clicks for the host to operate a live game. | DESIGN.md §2, `backend/src/services/gameLoop.service.ts` |
| 34 | `GameSession`/`Participant`/`Answer` are implemented as plain in-process objects/Maps (`backend/src/domain/sessionStore.ts`), with no new Prisma models | Matches Decision #16 exactly — these were already specified as in-memory-only. `ResultsSnapshot` (already in `schema.prisma`) is the only piece written to Postgres, once, at `game:ended`. | ARCHITECTURE.md §2, `backend/src/domain/sessionStore.ts` |
| 35 | Join-code brute-force lockout (DESIGN.md §8) is tracked by the **literal guessed join-code string**, in a module-level tracker, not as a `failedJoinAttempts` field on `GameSession` | Most brute-force guesses never resolve to a real session at all, so a per-session counter can't see them; keying by the raw guessed string catches both "hammering one specific active code" and repeated garbage guesses, and needs no session lookup to record a failure. Threshold/window/duration are the same shape DESIGN.md §8 described (20 failures / 5 min → 5 min lockout), just relocated. | DESIGN.md §8, `backend/src/domain/rateLimiter.ts`, `backend/src/sockets/session.socket.ts` |
| 36 | Session creation (`POST /quizzes/:id/sessions`) reuses the same placeholder `x-creator-id` header as the quiz-CRUD routes (Decision #28), rather than inventing a separate scheme | Consistent with the existing scaffolding; both will be replaced by real Creator auth in the same step (CLAUDE.md step 3, still not done). Confirmed with the project owner before implementing. | `backend/src/routes/session.route.ts` |
| 38 | Creator identity is a **capability bearer token**, not email/password or OAuth: `POST /auth/register` (no body) creates a `Creator` row and returns a random 32-byte token exactly once; only its SHA-256 hash is stored. All Creator-scoped routes now require `Authorization: Bearer <token>`, replacing the old `x-creator-id` header (Decision #28) | The placeholder header let any caller claim any creator id outright — a plaintext, unauthenticated assertion, not a credential. A bearer token closes that spoofing/hijack gap with no new infra (no email service, no OAuth dependency, no password to hash-and-salt) and reuses the same trust model as the session `hostToken`/`participantToken` already in the codebase. **Accepted trade-off:** no account recovery — losing the token (clearing storage, switching devices) permanently loses access to that Creator's quizzes; there is deliberately no "forgot my token" flow. `email`/`passwordHash` stay on the `Creator` model as nullable columns so a future passwordless-email upgrade (verify control of an inbox, for recovery/cross-device portability) can attach to an existing Creator without another breaking migration — not built, not planned unless asked. Registration itself is anonymous by construction, so — unlike other Creator endpoints, which fall under Decision #22's "already authenticated" exemption — it gets its own per-IP rate limit (10/hour) against runaway row creation. | ARCHITECTURE.md §7 (supersedes the email+password plan there for now), `backend/src/services/auth.service.ts`, `backend/src/routes/auth.route.ts`, `backend/src/routes/creatorAuth.ts`, `backend/prisma/migrations/0002_creator_capability_token/` |
| 37 | Full session/game-loop implemented: REST session creation, Socket.IO join/reconnect (idempotent via participant token), host/display auth, question broadcast with per-session-shuffled option order, `answer:submit` scoring per DESIGN.md §5, promotion request/decision flow, and `game:ended` → `ResultsSnapshot` write + paginated `GET /results/:sessionId` (ranks 4-10 on page 1, 11+ paginated per PRD §6) | This is CLAUDE.md steps 4 and 5, done together per the project owner's request. Verified end-to-end against a throwaway local Postgres container (migrations applied cleanly) and a real Socket.IO client driving a full two-question game (join → start → answer → lock/reveal/leaderboard → advance → end → results fetch), plus the join-code lockout (confirmed: exactly 20 failures allowed, 21st locked out). | `backend/src/services/gameLoop.service.ts`, `join.service.ts`, `results.service.ts`, `session.service.ts`; `backend/src/sockets/session.socket.ts`, `sockets/index.ts`; `backend/src/domain/` (constants, scoring, joinCode, rateLimiter, sessionStore, shuffle); `backend/src/routes/session.route.ts` |
| 39 | Frontend styling: **Tailwind CSS** | Project owner's choice; fast to build the visual/color-shape-grid-heavy views (PRD §4.3) without hand-rolling CSS or pulling in a full component library. | ARCHITECTURE.md §5, `docs/FRONTEND_PLAN.md` |
| 40 | Frontend data layer: **TanStack Query** for REST calls (Creator CRUD, results fetch) | Project owner's choice; handles caching/loading/error state for the REST side without hand-rolled fetch wrappers. Socket.IO-driven realtime state (game loop) is separate and not covered by this — still an implementation detail to work out when scaffolding starts. | ARCHITECTURE.md §5, `docs/FRONTEND_PLAN.md` |
| 41 | Frontend routing: **React Router**, matching the route table in ARCHITECTURE.md §5 | Project owner's choice; standard, well-supported fit for the role-driven multi-route SPA structure already decided (Decision #26). | ARCHITECTURE.md §5, `docs/FRONTEND_PLAN.md` |
| 42 | Socket.IO client access is centralized in a **shared client/hook module**, not duplicated per view | Project owner's choice; Host/Display/Play views all need the same participant-token reconnect handling (ARCHITECTURE.md §6) — one shared module keeps that logic in one place. | ARCHITECTURE.md §6, `docs/FRONTEND_PLAN.md` |
| 43 | Frontend testing: **Vitest** | Project owner's choice; pairs naturally with Vite (Decision #26/#44) without a separate test-runner config. | `docs/FRONTEND_PLAN.md` |
| 44 | Frontend scaffolded with **Vite**'s own scaffolding tool (`npm create vite@latest`, react-ts template) rather than a more opinionated starter | Project owner's choice; keeps the initial setup minimal and matches the already-decided React + Vite stack (ARCHITECTURE.md §1) with nothing extra to strip out later. | ARCHITECTURE.md §1, `docs/FRONTEND_PLAN.md` |
| 45 | Component layer on top of Tailwind: **shadcn/ui** (Radix UI primitives, source copied into the repo via its CLI rather than installed as an opaque dependency) | Project owner's choice; gives Creator-flow forms/modals/dropdowns accessible, sensibly-defaulted components that are still fully ownable/restylable with Tailwind, without a heavier all-in-one UI kit. | ARCHITECTURE.md §5, `docs/FRONTEND_PLAN.md` |
| 46 | Realtime game-loop state (current question/leaderboard/own score, fed by the shared Socket.IO client, Decision #42) lives in **plain React state/context**, not a separate state-management library, for now | Each client only ever tracks one active session's game-loop state at a time — no cross-tree global state sprawl — and TanStack Query (Decision #40) already owns REST state. **Documented fallback:** if Host/Display/Play state access becomes messier than clean context usage in practice (e.g. deep prop-drilling across unrelated trees), switch that slice to **Zustand** — not adopted now, revisit only if the plain-context approach actually proves painful. | ARCHITECTURE.md §5-§6, `docs/FRONTEND_PLAN.md` |
| 47 | Host Controller and Display views authenticate their socket connection via a **`?token=` URL query param** (`hostToken`/`displayToken` respectively) on `/host/:sessionId` and `/display/:sessionId`, with the Host page additionally accepting manual paste as a fallback | `POST /quizzes/:id/sessions` returns `hostToken` exactly once with no way to fetch it again, and there is no Creator-flow UI yet that starts a session and hands off to these routes (that's still open, see `docs/FRONTEND_PLAN.md`). This is an explicitly interim convention for both views (built in parallel worktrees, same interim answer reached independently) — replace with a direct in-app link once the Creator flow gains a "start session" action, rather than treating `?token=` as a permanent auth mechanism. | `frontend/src/pages/HostPage.tsx`, `frontend/src/pages/DisplayPage.tsx` |
| 48 | Answer option color/shape is assigned **client-side by option slot index** (`frontend/src/components/answerStyles.ts`, a fixed 4-slot Kahoot-style palette), not read from the server | The backend's `question:broadcast` payload only sends `{id, text}` per option — `DESIGN.md`'s event catalogue documents `shape`/`color` as part of that payload, but they were never implemented server-side. Since option order is already shuffled once per session (not per client, Decision #6), a fixed client-side slot→style mapping still satisfies the "everyone looking at the same shared screen sees the same shape" requirement. **Documented gap:** `DESIGN.md`'s event catalogue should eventually be reconciled with what the backend actually sends (or the backend changed to send it), but that's out of scope for the frontend work that surfaced it. | DESIGN.md event catalogue, `backend/src/services/gameLoop.service.ts` (`publicOptions`), `frontend/src/components/answerStyles.ts` |
| 49 | `/play/:sessionId`'s route param is actually the **join code**, not the session's real UUID | The backend's `join:accepted` socket event never returns the real session UUID — only `game:ended`'s `resultsUrl` does, later. `JoinPage` navigates to `/play/<joinCode>`, and `PlayPage` re-sends `join:request` with the stored participant token on mount using that same code, which also happens to serve as the reconnect-on-refresh path (ARCHITECTURE.md §6). Functionally fine since Play never needs the real UUID for anything itself, but worth knowing if a future view needs the real session id from the Play route. | `backend/src/sockets/session.socket.ts`, `frontend/src/pages/JoinPage.tsx`, `frontend/src/pages/PlayPage.tsx` |
| 50 | Local dev environment is a **docker-compose stack** (Postgres + backend dev server + frontend Vite dev server, each its own service/container), not native local installs | One-command spin-up (`docker compose up --build`) for manual and E2E testing without requiring a locally installed Postgres or matching Node version; each service still runs its normal dev command (`tsx watch`, `vite`) with the repo bind-mounted for live reload, so it isn't a production deployment artifact — ARCHITECTURE.md §4/§9's real hosting split is untouched. Backend image is `node:22-bookworm-slim` (not `-alpine`): Prisma's query engine needs glibc/OpenSSL, which Alpine's musl libc doesn't provide without extra packages, and Vite 8 requires Node ≥22. `backend-go/` is intentionally not included (Decision #31 — non-shipping exploration). | `docker-compose.yml`, `backend/Dockerfile`, `frontend/Dockerfile` |
| 51 | `apiFetch` (`frontend/src/lib/api.ts`) only sets `Content-Type: application/json` when a request actually has a body | Found via manual end-to-end testing against the docker-compose stack (Decision #50): the header was set unconditionally, and Fastify's JSON body parser rejects an empty body sent with that content-type (`FST_ERR_CTP_EMPTY_JSON_BODY`) — this broke the bodyless `POST /auth/register` call from `useRegisterCreator`, so the whole "create quiz" flow 400'd on a fresh Creator. Fixed by gating the header on `init.body !== undefined`. | `frontend/src/lib/api.ts` |

## How to add to this log

When a new design/architecture decision gets settled in conversation,
append a row here rather than letting it live only in chat history —
that's the whole point of this file for an agent picking up the project
cold.
